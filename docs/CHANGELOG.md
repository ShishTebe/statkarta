# Журнал изменений пакетов данных

## Приложение 0.6.1, данные 2026.3.1-draft – 22.09.2026 (замечания по проверке на Mac)

- «Открыть файл дела или документ»: принимает и файл дела, и документ (.docx, .txt, .rtf, .pdf) –
  по документу открывается новое дело с разбором на вкладке «Документ дела».
- Удаление события – в каждой строке таблицы событий (выставленные карточки – с предупреждением).
- Опросник: выбор карточки пакета и кнопка «Отложить вопросы этой карточки: уточнить позже»
  (откладываются только вопросы, которые не нужны другим карточкам).
- Ошибка бланка – кнопка «Исправить»: переход к вопросу этого реквизита этой карточки.
- Ф. 1 р. 40: добавлены местные подразделения из бланка ИЦ – 0018, 0017, 0019
  (`form-labels-2026.json`, раздел `options_add`).
- Ф. 1 р. 5 и ф. 5 р. 4: из номера КРСП «93пр-26» в клетки идет только номер (93), год не вписывается.
- Ф. 1 р. 12: фабула впечатывается шрифтом 6 пт; предел – 450 знаков (длиннее строка разрастается
  и сдвигает клетки р. 13); из постановления фабула сокращается до 450 знаков.
- Чтение потоков PDF совместимо с Safari.
- Опросник: вопросы в порядке реквизитов бланка карточки, с номером реквизита (замечание с Windows).
- Сервис-воркер загружает новую версию мимо кэша браузера.

## Приложение 0.6.0, данные 2026.3.0-draft – 20.09.2026 (Фаза 3, часть 1)

- Другие документы дела (FR-10): постановления о привлечении в качестве обвиняемого, о продлении
  срока, о приостановлении, о возобновлении, о прекращении, о признании потерпевшим. Вид документа
  определяется по заголовку, событие и реквизиты ф. 3 – по таблице `scripts/import/documents-2026.json`.
- Коды реквизитов ф. 3 подбираются по ссылке на УПК РФ в тексте (р. 12, 13, 17) и по сроку продления (р. 9).
- Форматы .rtf и .pdf с текстовым слоем (ответы В-35, В-46): при неразборчивом тексте или скане –
  сообщение «сохраните в Word»; предел размера файла – 30 МБ.
- Бланки карточек и карты раскладки опубликованы (ответ В-44): `data/blanks/2026`, `data/layout/2026`;
  из свойств файлов убраны фамилии сотрудников; аудит проверяет текст и свойства бланков.
- Шесть актов переведены в статус «публикуется» (ответ В-44).
- Лицензии: PolyForm Noncommercial 1.0.0 и CC0, соглашение с участниками (ответ В-43).
- Сборка для GitHub Pages (`npm run build:web`): работа без сети после первого открытия, значки,
  проверка обновлений по кнопке, напоминание 01.01 и 01.07 с файлом календаря .ics.

## Приложение 0.5.1, данные 2026.2.1-draft – 20.09.2026 (ответы В-40 – В-42)

- Потерпевшие из постановления о ВУД – подсказка (правило `x.vud.victims`): «потерпевшему …»,
  «причинив … физическую боль (вред)»; именительный падеж из родительного и дательного; в дело –
  только по отметке, объектом «Потерпевший».

## Приложение 0.5.0, данные 2026.2.0-draft – 20.09.2026 (Фаза 2 «Документы»)

- Прием постановления о ВУД: .docx и .txt (UTF-8, Windows-1251) без библиотек, вставка текста;
  .doc, .rtf, .pdf – сообщение.
- Правила извлечения ведутся в `scripts/import/extract-vud-2026.json` (14 правил): части
  постановления, уверенность по шаблону, исключения; номер дела – 17 цифр (В-34).
- Эпизоды и лица из резолютивной части, именительный падеж лица; перенос в дело только
  подтвержденного, событие «Возбуждение уголовного дела» с пакетом карточек.
- Вымышленный тестовый набор `tests/fixtures/vud` (24 постановления + 3 документа другого вида).
- Аудит публичных данных ищет и 17-значные номера дел.

## Приложение 0.4.4 – 19.09.2026

- Печать копий бланков всех карточек события одним проходом (A1.6-7).

## Приложение 0.4.3, данные 2026.1.6-draft – 19.09.2026 (ответ В-33)

- Ходатайство о судебном штрафе: комплект карточек как при направлении дела в суд – ф. 1.1, 2, 3, 4
  (при ущербе), ИПК-ЛЦ; в событии нужно указать эпизоды и лиц. Отмена постановления о прекращении –
  только ф. 3 (ф. 1.1 «изменить» не выставляется).

## Приложение 0.4.2, данные 2026.1.5-draft – 19.09.2026 (ответы В-30 – В-32)

- Ф. 3 р. 16 – снова наложение кодов по разрядам (В-30).
- Ф. 3: добавлены реквизиты бланка информационного центра, которых нет в PDF ГИАЦ: 7.2, 7.2.1, 18, 18.1
  (`form-corrections-2026.json`, раздел `additions`); р. 11 – дата. Все размечены в карте бланка.
- Наборы реквизитов ф. 3 по событиям уточнены (В-31); новые события: направление дела прокурору,
  ходатайство о судебном штрафе, принятие после возвращения на доп. расследование, отмена постановления
  о приостановлении или прекращении (В-32). Признак события `reopens` снимает срок хранения от
  предшествующего финального события.
- Контрольная печать Word и Excel новых мест бланков: `scripts/blanks/remarks_check.mjs`.

## Приложение 0.4.1, данные 2026.1.4-draft – 19.09.2026 (замечания по проверке на реальном деле)

- Контроль карточки: межкарточные правила (`scope: package`) больше не запускаются в памятке отдельной
  карточки – ошибка «функция package.has_form доступна только в проверках пакета» устранена.
- Раздел 1 (номер дела, КРСП, даты возбуждения и совершения) впечатывается в карточку и бланк, если
  сведения внесены следователем; без них реквизит остается работнику учета. Номер с разделителями
  раскладывается по клеткам цифрами. Размечены номер дела ф. 3 (р. 3), номер и дата КРСП ф. 1 (р. 5)
  и ф. 5 (р. 4); в ф. 5 исправлена разметка: клетки даты возбуждения (р. 3.2) были отнесены к р. 3.3.
- Код ОКАТО (11 разрядов) раскладывается по группам клеток 2-3-3-3 (ф. 1 р. 19.1) – печать больше
  не закрывается.
- ИПК: в бланке стояло «ПР» из образца; вид карты (ЛЦ или ПР) теперь впечатывается по варианту карточки.
  Лицо, указанное в событии без связи с эпизодами, связывается с эпизодами события; в пакете ВУД
  «в отношении лица» показывается предупреждение, если эпизод остался без лица (тогда выходит ИПК-ПР).
- Ф. 3: на каждое событие – свой набор реквизитов (`requisites`, `optional_requisites` в правиле события,
  общие – `card_common_requisites`); остальные реквизиты в карточке события не заполняются и не
  спрашиваются. Р. 8 – поле номера дела для всех трех кодов, номер подставляется из события соединения
  и из исходного дела при выделении; р. 16 – один вариант; варианты р. 7, 8, 12, 12.2, 13, 16, 17 получили
  полные формулировки и подсказки с основанием по УПК РФ (`form-labels-2026.json`, поле `hint`).
- Интерфейс: подсказка «?» у вариантов кодов (для всех форм – по ссылке на УПК РФ в тексте варианта),
  кнопка «Сегодня» у дат, выбранные коды и список «Показать все варианты» – по возрастанию кода, поле
  поиска по вариантам подписано.

## Приложение 0.4.0, данные 2026.1.3-draft – 16.09.2026 (Фаза 1.6 «Заполнение бланков»)

- Пакет эталонных бланков `data-private/blanks/2026`: 10 файлов (8 форм и две ИПК), образцы заполнения
  вычищены (`scripts/blanks/normalize.mjs`, список мест – `data-private/blanks-src/scrub-2026.json`),
  у каждого бланка контрольная сумма в `blanks.json`. Публикация бланков – по отдельному решению,
  поэтому пакет лежит в `data-private` и входит только в локальную сборку.
- Карты раскладки `data-private/layout/2026`: место в бланке задается порядковым номером фигуры;
  строятся полуавтоматически (`scripts/blanks/build_layout.py`: пометка мест → печать в PDF нативным
  Word → разбор расположения меток) и сверяются `scripts/check-layout.mjs`.
- Ядро: `zip.mjs` (чтение и сборка .docx без внешних библиотек), `ooxml.mjs` (правка мест бланка –
  фигуры VML и DrawingML, ячейки таблиц), `blank.mjs` (форматирование значений по клеткам, блокировка
  печати), `docx.mjs` (сборка заполненного файла с проверкой контрольной суммы).
- Интерфейс: на карточке – «Бланк карточки» (печатная копия, скачивание .docx, лист «Проверьте перед
  подписью»), в пакете – «Скачать бланки события» одним архивом.
- Подпись руководителя и прокурора в бланке по умолчанию не печатается (ответ В-14); значение, которое
  не входит в отведенные бланком клетки, не вписывается, печать закрывается до исправления (ответ В-19).

## Приложение 0.3.2, данные 2026.1.2-draft – 16.09.2026 (ответы на вопросы В-11 – В-13)

- Ф. 3 при возбуждении дела подтверждена как необязательная (В-11).
- Код органа прокуратуры и подпись прокурора применяются только к карточкам, отмеченным в профиле органа
  (`profile_fills.forms_from`, `profile_signatures`): в СК это р. 40 ф. 1 и р. 33 ф. 1.1, в других органах
  перечень задает пользователь (В-12).
- Подпись собирается одной строкой «должность звание фамилия И.О.» и выводится под памяткой и в печати:
  лицо, ведущее расследование; руководитель; прокурор – где он требуется (В-13).

## Приложение 0.3.1, данные 2026.1.1-draft – 16.09.2026 (замечания по Фазе 1.5)

- Ф. 3 при возбуждении дела не входит в обязательный пакет (уточнение порядка учета); добавляется вручную.
- Квалификация вводится по ячейкам бланка (статья, знак, часть, знак, пункты, стадия); разбор принимает пункты без кавычек.
- Профиль органа: код подразделения и код органа прокуратуры по справочнику № 17 вместо ОКАТО; должность, звание и
  фамилия следователя и руководителя – подставляются в реквизиты подписи ИПК и карты на иностранца.
- В бланках ф. 1 р. 40, ф. 1.1 р. 33, ф. 3 р. 15, ф. 4 р. 33, ф. 5 р. 21 учтены клетки «код подразделения»
  и «код органа прокуратуры»; значения подставляются из профиля.
- Источники правил в интерфейсе переформулированы без ссылок на заказчика.
- Темная тема: поля ввода и списки получили свой фон и цвет текста.

## Приложение 0.3.0, данные 2026.1.0-draft – 16.09.2026 (Фаза 1.5, пакет карточек по делу)

- Новый пакет `data/events/2026/events.json`: 12 событий дела, состав пакета карточек по каждому
  (подтвержден заказчиком 16.09.2026), признаки объектов (иностранный участник, вид ущерба),
  порядок первичности карточек для переносов, коды режима «Учесть, изменить, снять».
- Формы ИПК (варианты ЛЦ и ПР, 72 реквизита, 842 варианта кодов) и карта на преступление с участием
  иностранных граждан или ЛБГ (41 реквизит) – извлечены из эталонных бланков заказчика скриптом
  `scripts/import/import_ipk.py`; образцы заполнения в пакет не попадают (проверка `audit-public.mjs`).
- Наименования реквизитов ф. 2.1 (30) и ф. 6 (20) сверены с эталонными бланками; по ответу заказчика
  № 14 реквизит 15 ф. 6 заполняет следователь. Реквизиты ф. 6, которые заполняет следователь,
  и реквизиты ИПК получили переносы из общих сведений дела (номер дела, орган, квалификация,
  анкета лица, фабула, даты).
- У фактов и сопоставлений появилась область действия (`scope`), у контрольных соотношений – область
  `card` или `package` и обход `per`. Добавлены 7 межкарточных соотношений пакета.
- Квалификации нескольких эпизодов в карточке на лицо разбираются раздельно: стадия по ст. 30 УК РФ
  относится к своему эпизоду (исправление ошибки в строках квалификации ф. 2).

## Приложение 0.2.0, данные 2026.0.3-draft – 15.09.2026 (замечания заказчика по Фазе 1)

- Справочники пересобраны из машиночитаемых сборников ГИАЦ: «Справочники и разъяснения 2026» (№ 3, 9, 12, 14, 15)
  и сборника 2025 года (остальные, кроме № 5–7). У позиций – группа (например, «следователь: ФСБ»), сноски,
  признак исключения из редакции 2026. Новые коды: 000195, 144, 145, 286–288, 0600, 00219–00223, 000597.
- Разъяснения ГИАЦ к отдельным кодам и реквизитам (2025, 2026) – 64 новых абзаца с привязкой.
- Модель ввода реквизита по официальному бланку: число кодовых полей, режим выбора (один код, несколько,
  наложение по разрядам), дополнительные поля (суммы, количества), лимит фабулы 355 знаков.
- Подсказки кодов по статье и обстоятельствам (ИТТ, МКБ-10, несовершеннолетние, миграция, СВО, иностранцы),
  наложение подсказок направленности (10 + 02 = 12), неактивность характеристик при отсутствии потерпевших.
- Условно обязательные сведения по разъяснениям ГИАЦ и ГП, обязательный р. 40 ф. 1; контроль числа кодов и наложения.
- Поиск кода по неполному номеру (окончание, часть), кнопка «Показать все варианты».
- ОКАТО для р. 19.1 ф. 1, 28.1 ф. 2, 18.3 ф. 5: официальный набор Росстата на 01.04.2026, в сборке – Камчатский край (99 объектов).
- Исправлены переносы из верстки («следова- теля»), значения кодов с вложенными скобками (р. 24 ф. 1 код 04),
  прилипший номер реквизита в значениях (р. 25 ф. 1).

## Приложение 0.1.0 – 15.09.2026 (Фаза 1)

- Однофайловое офлайн-приложение: опросник и памятка для форм 1, 1.1, 2, 3; справочники с поиском;
  бланки с разъяснениями к реквизитам; нормативная база и чек-лист актуальности; печать и копирование
  памятки; удаление данных дела.
- Ядро: разбор квалификации, категория по перечням № 4–7 на дату преступления, стадия по ст. 30 УК РФ,
  подсказки направленности, строки квалификации по убыванию тяжести, 12 контрольных соотношений.
- Данные: общие вопросы о деле в `questions.json`; в членстве статей в перечнях – режим условий дат и
  охват (часть или статья); справочник № 17 в локальной сборке хранится компактно; висячие запятые в
  наименованиях справочников удалены.
- 19 автотестов, CI собирает приложение.

## 2026.0.2-draft – 15.09.2026

Устранены все предупреждения валидатора (было 30, стало 0).

- Формы: 37 исправлений по бланкам (подграфы таблиц квалификации ф. 2, 5, 6, строки изъятия
  ф. 4, реквизиты 25 и 25.1 ф. 1.1, графы наказаний ф. 6) – `scripts/import/form-corrections-2026.json`.
- Варианты кодов переразобраны из дословного текста: полные значения, группы кодов внутри
  реквизита (поле `group`), встроенные справочники ф. 6 (`embedded_classifiers`).
- Перечни УК: исправлены ошибки распознавания, условия «вне диапазона дат» (`date_mode: any`),
  ссылки классифицированы (`historical`, `stale_in_source`); дефектов 0.
- Валидатор проверяет повтор кодов внутри группы; схема формы дополнена.

## 2026.0.1-draft – 15.09.2026

Первая сборка пакетов данных (Фаза 0).

- Формы 1, 1.1, 2, 2.1, 3, 4, 5, 6 ред. 2026: 355 реквизитов. Исправлены артефакты исходной
  схемы: 8 пустых дублей удалено, 8 реквизитов перенумеровано, 7 типов полей исправлено
  (журнал – `docs/import-log-forms-2026.md`). Добавлен признак «кто заполняет».
- Справочники № 1–12, 14–16: 2 499 кодов. Справочник № 17: 28 503 записи, вынесен в закрытую часть.
- Статьи УК РФ: 418 статей, 1 111 частей (1 099 – действующих). Категория тяжести по перечням № 4–7 указания
  от 28.07.2025 № 147 (скан распознан локально) со сверкой по санкциям УК.
- Перечни статей УК № 1–14, 20–26 с условиями по дате и дополнительными условиями.
- Нормативная база: 7 актов; 52 абзаца разъяснений ГП на 01.07.2025 с привязкой к реквизитам.
- Правила (черновик): 218 фактов, 210 вопросов, 355 сопоставлений, 4 производных значения,
  12 проверок, 9 правил извлечения из постановления о ВУД, 10 типов документов.
- JSON-схемы, манифест с контрольными суммами, валидатор, аудит публичных данных, CI.
